{
  "name": "moodmash",
  "type": "module",
  "version": "1.0.0",
  "description": "MoodMash - Intelligent Mood Tracking & Wellness Platform",
  "scripts": {
    "dev": "vite",
    "dev:sandbox": "wrangler pages dev dist --d1=moodmash --local --ip 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name moodmash",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:migrate:local": "wrangler d1 migrations apply moodmash --local",
    "db:migrate:prod": "wrangler d1 migrations apply moodmash",
    "db:seed": "wrangler d1 execute moodmash --local --file=./seed.sql",
    "db:reset": "rm -rf .wrangler/state/v3/d1 && npm run db:migrate:local && npm run db:seed",
    "db:console:local": "wrangler d1 execute moodmash --local",
    "db:console:prod": "wrangler d1 execute moodmash",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:codegen": "playwright codegen http://localhost:3000",
    "test:homepage": "playwright test tests/e2e/01-homepage.spec.ts",
    "test:log": "playwright test tests/e2e/02-log-mood.spec.ts",
    "test:activities": "playwright test tests/e2e/03-activities.spec.ts",
    "test:i18n": "playwright test tests/e2e/04-internationalization.spec.ts",
    "test:pwa": "playwright test tests/e2e/05-pwa-service-worker.spec.ts",
    "test:chatbot": "playwright test tests/e2e/06-chatbot-accessibility.spec.ts",
    "test:onboarding": "playwright test tests/e2e/07-onboarding.spec.ts",
    "git:init": "git init && git add . && git commit -m 'Initial commit'",
    "git:commit": "git add . && git commit -m",
    "git:status": "git status",
    "git:log": "git log --oneline"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@hono/oauth-providers": "^0.8.5",
    "@sentry/browser": "^10.27.0",
    "@sentry/cloudflare": "^10.27.0",
    "arctic": "^3.7.0",
    "bcryptjs": "^3.0.3",
    "hono": "^4.10.4",
    "oslo": "^1.2.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1",
    "typescript": "^5.0.0",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
