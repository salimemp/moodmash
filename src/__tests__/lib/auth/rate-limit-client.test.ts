// This file exists for backward compatibility
// Tests have been split into smaller, more focused files:
// - src/__tests__/lib/auth/rate-limit-client/throttle.test.ts
// - src/__tests__/lib/auth/rate-limit-client/withBackoff.test.ts

// Import vitest utilities
import { describe, it } from 'vitest';

describe('Rate Limit Client', () => {
  // Note: Actual tests have been moved to dedicated files.
  // This file is kept for backward compatibility only.
  it('has moved tests to dedicated files', () => {
    // This test just exists to make sure this file passes
    // and to document that tests were moved to separate files
  });
}); 